# 校园综合服务平台 — 系统架构设计文档

> 版本：v2.0 | 更新：2026-06-06 | 服务器：101.37.182.56 (阿里云 ECS)

---

## 一、系统架构总览

```
用户浏览器 (Vue 3 SPA)
    │
    ▼
Nginx (:80)  ─── 静态文件 (Vite build dist/)
    │            ─── /api/* → Gateway
    │            ─── /uploads/* → 直接服务文件
    │
    ▼
Gateway (:8080) ─── Spring Cloud Gateway + Netty
    │               ├── AuthGlobalFilter (JWT + 角色鉴权)
    │               ├── TraceIdFilter (全链路追踪)
    │               └── XssFilter (安全过滤)
    │
    ├── user-service (:8082)        ├── forum-service (:8084)
    ├── campus-news-service (:8083) ├── lost-found-service (:8085)
    ├── second-hand-service (:8086) └── complaint-service (:8087)
    │
    ├── Nacos (:8848)  ─── 服务注册/发现 + 配置中心
    ├── Redis (:6379)  ─── Token缓存/点赞/收藏/浏览量/禁言
    └── MySQL (:3306)  ─── 各服务独立数据库
```

---

## 二、微服务拓扑

| 服务 | 端口 | 数据库 | 依赖 |
|------|------|--------|------|
| campus-gateway | 8080 | - | Nacos |
| user-service | 8082 | campus_user | MySQL, Nacos |
| campus-news-service | 8083 | campus_news | MySQL, Nacos |
| forum-service | 8084 | campus_forum | MySQL, Redis, Nacos |
| lost-found-service | 8085 | campus_lostfound | MySQL, Nacos |
| second-hand-service | 8086 | campus_secondhand | MySQL, Redis, Nacos |
| complaint-service | 8087 | campus_complaint | MySQL, Nacos |

---

## 三、分层架构（以 forum-service 为例）

```
Controller 层 ─── PostController, CommentController
    │              接收 HTTP 请求，参数校验，返回 Result<T>
    ▼
Service 层 ─── PostService, CommentService
    │           核心业务逻辑，@Transactional，Redis 操作
    ▼
DAO 层 ─── PostMapper, CommentMapper
    │           MyBatis-Plus BaseMapper
    ▼
数据层 ─── MySQL (持久化) + Redis (点赞/收藏/禁言)
```

**包结构**：
```
forum-service/src/main/java/com/campus/forum/
├── ForumApplication.java
├── controller/  (PostController, CommentController, NotificationController)
├── service/     (接口 + impl/)
├── mapper/      (MyBatis-Plus Mapper)
├── entity/      (Post, Comment, Notification)
├── config/      (Redis 配置)
├── task/        (定时任务：Redis → MySQL 同步)
└── constant/    (Redis Key 常量)
```

---

## 四、核心技术选型

| 组件 | 版本 | 用途 |
|------|------|------|
| JDK | 17 | 运行环境 |
| SpringBoot | 3.1.5 | 应用框架 |
| SpringCloud Gateway | 2022.0.x | API 网关 |
| Nacos | 2.5.2 | 服务注册 + 配置中心 |
| Sentinel | 1.8.8 | 熔断降级 |
| MyBatis-Plus | 3.5.5 | ORM + 分页 |
| MySQL | 8.0 | 数据持久化 |
| Redis | 7.x | 缓存/点赞/收藏/禁言 |
| JJWT | 0.12.3 | JWT 令牌 |
| Vue | 3.4 | 前端框架 |
| Vite | 5.x | 构建工具 |
| Vant | 4.8 | UI 组件库 |
| Pinia | 2.x | 状态管理 |
| Nginx | 最新 | 反向代理 + 静态文件 |
| Docker | 29.x | 容器化部署 |

---

## 五、安全架构

```
请求 → Gateway AuthGlobalFilter
         │
         ├── 白名单？ → 放行
         ├── 提取 Bearer Token → JWT 解析
         ├── 单设备登录检查 (ConcurrentHashMap: userId → loginTime)
         ├── 角色鉴权 (teacherPaths / adminPaths)
         ├── 注入 Header: X-User-Id, X-Nickname, X-User-Role
         └── 转发微服务
```

**JWT 结构**：
```json
{
  "sub": "1",
  "username": "admin",
  "nickname": "管理员",
  "role": "admin",
  "iat": 1717408000,
  "exp": 1718012800
}
```

---

## 六、数据库设计

### 各服务独立数据库

| 数据库 | 主要表 |
|--------|--------|
| campus_user | t_user |
| campus_news | t_news |
| campus_forum | t_post, t_comment, t_notification, t_user_mute |
| campus_lostfound | t_lost_found |
| campus_secondhand | t_second_hand, t_message, t_order |
| campus_complaint | t_complaint |

### 核心表结构

**t_user**：id, username, password(BCrypt), nickname, phone, email, avatar_url, role(student/teacher/admin), college, major, grade, status, deleted, create_time, update_time

**t_post**：id, title, content, images(JSON), board(study/job/life/tech/other), author_id, author_name, view_count, like_count, comment_count, is_pinned, status, deleted, create_time, update_time

**t_comment**：id, post_id, parent_id, content, author_id, author_name, status, create_time

**t_notification**：id, user_id, type, title, content, related_id, is_read, create_time

**t_user_mute**：id, user_id(UNIQUE), muted_until, reason, create_time

**t_second_hand**：id, title, description, category, images(JSON), price, original_price, goods_condition(new/like_new/good/fair), status(active/sold/offline), seller_id, seller_name, contact, view_count, fav_count, deleted, create_time, update_time

**t_message**：id, goods_id, sender_id, sender_name, receiver_id, content, is_read, create_time

**t_order**：id, buyer_id, goods_id, goods_title, amount, seller_name, status, create_time

**t_complaint**：id, type(repair/complaint/suggest), title, description, images(JSON), location, status(pending/processing/done/rejected), user_id, user_name, handler_id, handler_name, reply, rating, feedback, deleted, create_time, resolve_time, update_time

---

## 七、关键业务流

### 二手交易购买流程
```
浏览商品 → 商品详情 → [立即购买] → 选择支付方式 → 确认 → 创建订单 → 商品标记已售
```

### 论坛禁言流程
```
管理员双击回复 → 查禁言状态API → 显示[禁言/解除禁言]
  → 禁言：选时长(分钟~10年) → POST /comment/mute → 写入t_user_mute
  → 解除：确认 → DELETE /comment/mute/{userId}
被禁言用户回复 → 403 "你已被禁言，剩余X分钟后解除"
到期自动解除（muted_until > NOW() 判断）
```

### 单设备登录流程
```
设备1登录 → JWT含iat → 首次请求设置loginSessions[userId]=iat
设备2登录 → 新iat > 旧iat → 覆盖session
设备1下次请求 → iat < session中的值 → 401 "账号已在另一台设备登录"
前端每5秒心跳检测 → 401自动跳登录页
```

---

## 八、部署架构

### 阿里云 ECS（生产环境）
```
Docker 容器:
  ├── mysql:8.0 (3306)
  ├── redis:7-alpine (6379)
  └── nacos/nacos-server:v2.3.1 (8848)

宿主机 Java 进程:
  ├── gateway.jar (:8080)
  ├── user-service.jar (:8082)
  ├── news-service.jar (:8083)
  ├── forum-service.jar (:8084)
  ├── lost-found-service.jar (:8085)
  ├── second-hand-service.jar (:8086)
  └── complaint-service.jar (:8087)

Nginx (:80): 前端静态文件 + /api/* 反向代理 + /uploads/* 文件服务
```

### 本地开发环境
```
E:\ck\start-all.bat 一键启动:
  1. 检查 MySQL/Redis/Nacos
  2. mvn spring-boot:run 启动 6 微服务 + Gateway
  3. npx vite --port 3000 启动前端
访问: http://localhost:3000
```

### Docker 本地部署（可选）
```
E:\ck\docker-local\docker-compose.yml
  所有服务容器化运行
  MySQL:3307 Redis:6380 Nacos:8848 (避免端口冲突)
```

---

## 九、项目文件结构

```
E:\ck\
├── campus-platform/          # 父工程 + 各微服务模块
├── sql/                      # 数据库初始化脚本
├── deploy/                   # 阿里云部署脚本 + Dockerfile
├── docker-local/             # 本地 Docker 部署配置
├── start-all.bat             # 本地一键启动
├── stop-all.bat              # 本地一键停止
├── 需求文档-校园综合服务平台.md
└── 架构设计文档-校园综合服务平台.md
```
